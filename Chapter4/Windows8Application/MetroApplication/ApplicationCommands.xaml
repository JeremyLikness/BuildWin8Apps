<UserControl
    x:Class="Windows8Application.ApplicationCommands"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows8Application"
    d:DesignWidth="1024"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="LeftBar" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="Delete" HorizontalAlignment="Right" Style="{StaticResource DeleteAppBarButtonStyle}" Click="Delete_Click_1"/>
        </StackPanel>
        <StackPanel x:Name="RightBar" Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1">
            <Button x:Name="Add" HorizontalAlignment="Right" Style="{StaticResource AddAppBarButtonStyle}" Click="Add_Click_1"/>
            <Button x:Name="Home" HorizontalAlignment="Right" Style="{StaticResource HomeAppBarButtonStyle}" Click="Home_Click_1"/>
        </StackPanel>
    </Grid>
</UserControl>
