<Page
    x:Name="pageRoot"
    x:Class="Windows8Application2.ExtendedSplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows8Application2"
    xmlns:common="using:Windows8Application2.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Canvas Background="White">        
        <Image x:Name="SplashImage" Stretch="None" Source="/Assets/SplashScreen.png"/>
        <ProgressRing x:Name="Progress" IsActive="True" IsEnabled="True" Foreground="Green"/>
    </Canvas>
</Page>
